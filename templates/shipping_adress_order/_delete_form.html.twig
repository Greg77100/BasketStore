<form method="post" action="{{ path('app_shipping_adress_order_delete', {'id': shipping_adress_order.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shipping_adress_order.id) }}">
    <button class="btn">Delete</button>
</form>
