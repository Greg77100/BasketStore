{% extends 'base.html.twig' %}

{% block h1 %}<h1 class="title">Adresse de facturation</h1>{% endblock %}

{% block body %}
    

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ billing_adress.id }}</td>
            </tr>
            <tr>
                <th>FullName</th>
                <td>{{ billing_adress.fullName }}</td>
            </tr>
            <tr>
                <th>StreetAdress</th>
                <td>{{ billing_adress.streetAdress }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ billing_adress.city }}</td>
            </tr>
            <tr>
                <th>PostalCode</th>
                <td>{{ billing_adress.postalCode }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ billing_adress.phone }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ billing_adress.createdAt ? billing_adress.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_billing_adress_index') }}">back to list</a>

    <a href="{{ path('app_billing_adress_edit', {'id': billing_adress.id}) }}">edit</a>

    {{ include('billing_adress/_delete_form.html.twig') }}
{% endblock %}
