{% extends 'base.html.twig' %}

{% block h1 %}<h1 class="title">Confirmation de commande</h1>{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>Merci pour votre commande ! 🙏</h1>
        <p class="lead">Votre numéro de commande est : <strong>#{{ order.id }}</strong></p>

        <h3 class="mt-4">Détails :</h3>
        <ul class="list-group">
            {% for item in order.orderList %}
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    {{ item.product.title }} × {{ item.quantity }}
                    <span>{{ (item.product.price * item.quantity)|number_format(2, '.', ',') }} €</span>
                </li>
            {% endfor %}
        </ul>

        <h4 class="text-end mt-3">Total : {{ order.totalOrder|number_format(2, '.', ',') }} €</h4>

        <a href="{{ path('app_catalog') }}" class="btn btn-primary mt-4">Retour au catalogue</a>
    </div>
{% endblock %}
