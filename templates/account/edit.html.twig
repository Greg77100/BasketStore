{% extends 'base.html.twig' %}
{% block h1 %}<h1 class="title">Modifier mes informations</h1>{% endblock %}
{% block body %}

    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('app_account') }}">Mon compte</a></li>
            <li class="breadcrumb-item active" aria-current="page">Modifier mes informations</li>
        </ol>
    </nav>

{{ form_start(form) }}
    {{ form_row(form.firstname) }}
    {{ form_row(form.lastname) }}
    {{ form_row(form.email) }}
    <button class="btn btn-primary">Enregistrer</button>
{{ form_end(form) }}
{% endblock %}
