{% extends 'base.html.twig' %}
{% block h1 %}<h1 class="title">Ajouter une adresse de facturation</h1>{% endblock %}
    
{% block body %}

    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('app_account') }}">Mon compte</a></li>
            <li class="breadcrumb-item active" aria-current="page">Ajouter une adresse de facturation</li>
        </ol>
    </nav>
    {{ form_start(form) }}
        {{ form_row(form.fullName) }}
        {{ form_row(form.streetAdress) }}
        {{ form_row(form.city) }}
        {{ form_row(form.postalCode) }}
        
        <button class="btn btn-primary">Ajouter</button>
    {{ form_end(form) }}
{% endblock %}