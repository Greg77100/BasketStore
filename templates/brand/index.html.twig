{% extends 'base.html.twig' %}

{% block h1 %}<h1 class="title">Gestion des marques ({{brands|length}})</h1>{% endblock %}

{% block body %}

     <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('app_admin') }}">Back office</a></li>
            <li class="breadcrumb-item active" aria-current="page">Gestion des marques</li>
        </ol>
    </nav>


    <a class="btn btn-dark" href="{{ path('app_brand_new') }}">Ajouter une marque</a>


    <table class="table table-striped table-hover table-bordered text-center mt-4">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nom</th>
                <th>Quantité</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for brand in brands %}
            <tr class="align-middle">
                <td>{{ brand.id }}</td>
                <td>{{ brand.name|capitalize }}</td>
                <td>{{ brand.products|length }}</td>
                <td>
                    <a href="{{ path('app_brand_show', {'id': brand.id}) }}"
                    style="text-decoration: none; color: inherit;">
                    👀
                    </a>
                    <a href="{{ path('app_brand_edit', {'id': brand.id}) }}"
                    style="text-decoration: none; color: inherit;">
                    ✏️
                    </a>
                    {{ include('brand/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Aucun résultat</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
