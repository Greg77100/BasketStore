{% extends 'base.html.twig' %}

{% block title %}BillingAdressOrder{% endblock %}

{% block body %}
    <h1>BillingAdressOrder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ billing_adress_order.id }}</td>
            </tr>
            <tr>
                <th>FullName</th>
                <td>{{ billing_adress_order.fullName }}</td>
            </tr>
            <tr>
                <th>StreetAdress</th>
                <td>{{ billing_adress_order.streetAdress }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ billing_adress_order.city }}</td>
            </tr>
            <tr>
                <th>PostalCode</th>
                <td>{{ billing_adress_order.postalCode }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ billing_adress_order.phone }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ billing_adress_order.createdAt ? billing_adress_order.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_billing_adress_order_index') }}">back to list</a>

    <a href="{{ path('app_billing_adress_order_edit', {'id': billing_adress_order.id}) }}">edit</a>

    {{ include('billing_adress_order/_delete_form.html.twig') }}
{% endblock %}
