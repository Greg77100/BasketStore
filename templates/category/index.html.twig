{% extends 'base.html.twig' %}

{% block h1 %}<h1 class="title">Gestion des catégories({{categories|length}})</h1>{% endblock %}

{% block body %}

    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('app_admin') }}">Back office</a></li>
            <li class="breadcrumb-item active" aria-current="page">Gestion des categories</li>
        </ol>
    </nav>


    <a class="btn btn-dark" href="{{ path('app_category_new') }}">Ajouter une categorie</a>

    <table class="table table-striped table-hover table-bordered text-center mt-4">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Quantité</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr class="align-middle">
                <td>{{ category.id }}</td>
                <td>{{ category.name|capitalize }}</td>
                <td>{{ category.products|length }}</td>
                <td>
                    <a href="{{ path('app_category_show', {'id': category.id}) }}"
                    style="text-decoration: none; color: inherit;">
                    👀  
                    </a>
                    <a href="{{ path('app_category_edit', {'id': category.id}) }}"
                    style="text-decoration: none; color: inherit;">
                    ✏️ 
                    </a>
                    {{ include('category/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun résultat</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
