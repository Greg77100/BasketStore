{% extends 'base.html.twig' %}

{% block title %}OrderList{% endblock %}

{% block body %}
    <h1>OrderList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_list.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ order_list.quantity }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ order_list.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_order_list_index') }}">back to list</a>

    <a href="{{ path('app_order_list_edit', {'id': order_list.id}) }}">edit</a>

    {{ include('order_list/_delete_form.html.twig') }}
{% endblock %}
